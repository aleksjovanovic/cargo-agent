version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/migrations/queries.sql"
    schema: "internal/migrations/schema.sql"
    gen:
      go:
        package: "store"
        out: "internal/store"
        emit_json_tags: true
        emit_prepared_queries: true
        emit_empty_slices: true
        overrides:
          - db_type: "user_status"
            go_type: "github.com/aleksjovanovic/cargo-agent/internal/models.UserStatus"


